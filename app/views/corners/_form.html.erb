<%= form_with(model: corner) do |form| %>
  <% if corner.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(corner.errors.count, "error") %> prohibited this corner from being saved:</h2>

      <ul>
        <% corner.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :velocity, style: "display: block" %>
    <%= form.text_field :velocity %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
